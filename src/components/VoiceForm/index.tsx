import React from 'react';
import classes from './styles.module.scss';

const VoiceForm: React.FC = () => {
  return <div className={classes['voice-form']}>VoiceForm</div>;
};

export default VoiceForm;
